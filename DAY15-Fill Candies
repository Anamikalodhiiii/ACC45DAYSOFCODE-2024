#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int N, K, M;
        cin >> N >> K >> M;

        int bags = (N + K * M - 1) / (K * M);

        cout << bags << endl;
    }

    return 0;
}
