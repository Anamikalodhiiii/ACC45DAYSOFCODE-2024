#include <iostream>
using namespace std;

int countChoices(int n) {
    
    return n * (n - 1);
}

int main() {
    int t;  
    cin >> t;

    while (t--) {
        int n;  
        cin >> n;

        cout << countChoices(n) << endl;
    }

    return 0;
}
