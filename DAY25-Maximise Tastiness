#include <iostream>
using namespace std;

int main() {
    int t;  
    cin >> t;

    while (t--) {
        int a, b, c, d;  
        cin >> a >> b >> c >> d;

        int maxTastiness = max(a, b) + max(c, d);

        cout << maxTastiness << endl;
    }

    return 0;
}
