#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
  
    while (T--) {
        int B1, B2, B3;
        cin >> B1 >> B2 >> B3;
        cout << ((B1 + B2 + B3 < 2) ? "Water filling time" : "Not now") << endl;
    }

    return 0;
}
