#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int n, x;
        cin >> n >> x;

        int subscriptionsNeeded = (n + 5) / 6; 
        int totalCost = subscriptionsNeeded * x;

        cout << totalCost << endl;
    }

    return 0;
}
